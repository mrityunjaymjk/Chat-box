{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is the node server for chat chat app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mrityunjay (mj)",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.7.2"
  }
}
